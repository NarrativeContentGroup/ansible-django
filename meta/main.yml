---
dependencies:

  - role: 'git+https://github.com/MotherNatureNetwork/ansible-nginx.git,v1.0.1,mnn.nginx'
    sudo: yes

  - role: theonion.uwsgi-emperor
    uwsgi_uid: "{{ ansible_ssh_user }}"
    uwsgi_gid: "{{ ansible_ssh_user }}"
    sudo: yes
